apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheeld.fullname" . }}-web
  labels:
    {{- include "sheeld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheeld.web.selectorLabels" . | nindent 4 }}
