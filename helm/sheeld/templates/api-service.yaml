apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheeld.fullname" . }}-api
  labels:
    {{- include "sheeld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheeld.api.selectorLabels" . | nindent 4 }}
